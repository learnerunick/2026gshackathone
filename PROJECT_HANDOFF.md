# GS그룹 랄프톤 프로젝트 인계 문서

작성 기준일: 2026-09-21 (한국 시간)

## 현재 인계 요약 — AI 전체 제작 루프·상세 로그·자동 영상

**이 절과 최신 사용자 요청이 아래의 과거 인계·구현 기록보다 우선한다.**

- **23:12 KST Goal 완료 기준 검증:** 같은 persona v3의 실제 4건(c0001~c0004)이 연속 ready 등록됐다. 이미지 캐러셀 3건·영상 1건이며 실제 표시/재생/다운로드·출처·외형·한국어 음성/자막을 확인했다. c0004 「카레는 좋은데, 밥 말고」가 GS25 카레 당면을 구체적으로 연결한다. 잘못 생성된 포장 문구를 내장 image_gen으로 수정하고 AI 재현 고지를 넣은 v2를 원본 보존 후 등록했다. 3장 로드·ZIP 해시·피드 표시·콘솔 0건을 확인했다. `final-audit.json`은 운영·격리 DB, 저장 리비전 7개, 모든 미디어/실제 ZIP, 제어/재시작 증거와 설정을 다시 대조해 통과했다. Python 245개·UI 15개 통과 기록은 유효하다. **사람 승인 대기·AI 품질 검수 보류·기존 비용 한도는 유지**한다. 포장 세부 글씨의 AI 재현 한계와 영상 테이프 소품의 연속성 한계는 검토 메모에 남겼다. 기존 무제한 실행은 사용자 설정대로 유지하고 이후 결과는 이번 검증에 포함하지 않는다. [최종 검증·제한](docs/goal-validation-2026-09-21.md).


- **22:43 KST 상품 근거·버전 표시 업데이트:** c0004는 「카레는 좋은데, 밥 말고」 3장 이미지 기획을 마치고 storyboard에 있다. GS25 ‘카레 당면’의 2026-09-17 공식 본문·포장 원본을 별도 브라우저로 확인했고 `source-review-c0004.json`에 채택 범위를 기록했다. 현재 재고·구매·시식 경험은 주장하지 않는다. 실제 이미지·문안 확인은 아직 남는다. 영상 라이브러리에는 같은 제목의 원본·수정본을 구분하도록 목록/상세/접근성 이름에 콘텐츠 버전과 현재·이전 표시를 추가했다(`apps/dashboard/app.js`). 새 외부 호출이나 서버 재시작 없이 반영되며 브라우저에서 각각의 다운로드 주소 유지, 390px 가로 넘침 없음, 콘솔 오류 0건, JS 구문·UI 15개 통과를 확인했다. 격리 DB의 승인/수정/반려·목표 종료·중지·7개 리비전 해시도 `persisted-control-audit.json`으로 다시 대조했다.

- **22:31 KST 실제 3건 등록·영상 수정 검증:** c0001·c0002 이미지 및 c0003 영상이 같은 persona v3로 연속 ready 등록됐다. 새 영상의 한국어 3문장은 대본 주입 없는 ASR에서 모두 일치했다. 원본의 접힌 상자 선등장과 자막 조기 종료를 발견해 추가 유료 생성 없이 구간 편집·발화 시각 정렬로 고치고, 원본 v1을 보존한 v2로 등록했다. 20.04초 전편 브라우저 재생·자동 갱신 유지·MP4/ZIP 다운로드 원본 해시·Range 206을 확인했다. 테이프 보조 소품의 연속성 한계는 검토 메모에 남겼고 사람 승인·AI 검수 보류 설정을 유지한다. `content-review-c0003.json`, `import-c0003-repair.json`, `browser-c0003-ready.log`, `export-c0003.json`이 증거다. **전체 Goal은 진행 중**: 특정 GS 상품 연결을 검증할 `brief-d7ab5d7030d94343`은 c0004 sources에서 producing이며, 이 실제 결과 확인이 남는다. 서버 6150 유지, 과거 사용자 중지 실행도 stopped 유지.

- **22:22 KST Goal 재점검:** 셋째 「문을 끝까지 열고 싶어서」는 한국어 대사 3개를 고정하고 실제 Segmind 작업 `video-acc1a13582344a808c124c3100e1d276` / 외부 ID `5a13d7d0c3185549a9c6527eeac322f1`로 생성 중이다. 기존 ID 조회만 유지하며 예상 $2.13, 당일 누계 $6.39로 한도 변경은 없다. 서버 PID 6150이 살아 있고 최신 조회 오류는 0회다. 앞선 이미지 2건은 브랜드 공식 자료를 내부 영감으로 썼으므로 특정 GS 상품의 자연스러운 등장 검증까지 완료했다고 판단하지 않는다. 이 조건을 확인할 실제 이미지 기획 `brief-d7ab5d7030d94343`을 같은 실행의 다음 순환에 접수했다(`output/goal-validation/product-story-brief*.json`). 전체 Goal은 계속 진행 중이며 실제 영상 음성·자막·재생·다운로드와 추가 상품 연결 결과 확인이 남는다.

- **21:50 KST 실제 검증 업데이트:** 같은 페르소나 v3의 이미지 콘텐츠 2건이 ready v1로 등록됐다. 두 결과의 이미지 총 6장·문안·출처·인물/소품 연속성·검수/피드 표시·ZIP 원본 해시를 확인했다. 두 번째는 「책상도 퇴근할 시간」이며 손 방향·구도·빛 범위의 차이를 검토 메모로 남겼다. `completed_cards[].file_path` 미리보기 수정도 21:48 KST 안전한 순환 경계에 반영 완료했다(`output/goal-validation/preview-file-path-rollout.json`, 서버 PID 6150). Goal의 영상 조건을 위해 넣은 실제 기획 `brief-9b66169381a448e8`은 셋째 순환 c0003에 연결돼 소재 조사 중이다. 전체 Goal은 아직 진행 중이며 영상 생성·재생·한국어 음성/자막 검증이 남아 있다.

- **Goal 구현·기능 검증:** 구조형 외형 편집에서 지침이 사라지는 문제와 native 이미지 생성 중간 파일이 단계 미리보기에 나오지 않는 문제를 고쳤다. 완료 호출 receipt와 `completed_cards[].file_path`를 지원하며 경로·해시 검증을 유지한다. Python 245개·UI 15개 및 격리 8775 환경의 프로필·승인/재승인·반려·미디어·실행 제어·서버 재시작 검증을 확인했다. 첫째·둘째 순환 완료 시 자체 유지보수 일시정지·백업·서버 교체·재개로 수정 사항을 반영했다. 기존 사용자 중지 실행과 사람 승인 상태는 유지했다. 합성 3순환은 실제 AI 결과 3건으로 계산하지 않는다. [완료 기준별 증거와 남은 작업](docs/goal-validation-2026-09-21.md).

- **연속 실행 최종 점검:** 동일 외부 영상의 실제 완료를 확인한 뒤에도 run이 blocked로 남는 경로, 조회 불확실성 이후 공급자의 확정 실패를 처리하지 못하는 경로를 보완했다. 현재 요청·콘텐츠·배정을 검증하고 단계 결과 반영과 해당 차단 해제를 원자적으로 수행한다. 사용자 중지·일시정지·다른 차단·3회 연속 실패 제한은 유지한다. 실제 실행 관리자와 테스트용 자식 프로세스로 3개 콘텐츠·18단계 연속 제작 및 사람 승인 대기와 다음 제작의 독립성을 확인했다. Python 242개 통과, 서버 반영 정보는 `.runtime/continuity-rollout.json`. 실제 실행은 사용자 중지를 유지했고 유료 호출·SNS 게시 없음. [상세 점검](docs/continuous-production-review-2026-09-21.md).

- **AI 품질 검수(기존 6단계)는 사용자 요청으로 보류했다.** 신규 실행 기본값은 `ai_quality_review_enabled:false`, 활성 흐름은 소재 → 기획 → 스토리보드 → 문안 → 미디어 → 피드 등록의 6단계다. 기존 DB 단계 인덱스는 유지한다. 미디어 완료 직후 register로 이동하고 `review_handoff`와 `stage.skipped`에 보류 사유를 남긴다. import는 보류 메모를 `review_notes`에 자동 첨부한다. AI 통과·사람 승인을 조작하지 않으며 출처·미디어 검증, 사람 수정본 보호, 버전별 승인·게시 제한은 그대로다. 아래의 필수 AI quality/voice 검수 서술은 해당 검수를 켠 과거 실행에만 적용한다.
- 기존 활성 실행용 `run-defer-quality RUN_ID`를 추가했다. 검수에서만 막힌 실행은 원본 미디어를 확인한 뒤 register로 넘길 수 있다. 과거 오류·배정·결과를 보존하고 이전 검수 lease는 무효화한다. 다른 단계의 불확실성, 사용자 일시정지·중지, 목표 개수·비용 한도는 해제하지 않는다.
- **실제 적용 중 사용자가 20:31에 기존 실행 `run-fdb94de359954c75`를 중지했다.** 따라서 이 실행을 재개하거나 피드를 새로 등록하지 않았다. 완성 영상 2개와 과거 검수 기록을 보존했고 신규 자동 제작부터 새 정책을 사용한다. 8765 서버를 새 코드로 재기동했으며 배포·백업 정보는 `.runtime/deferred-quality-rollout.json`에 있다. Python 237개와 UI 11개 통과, 브라우저에서 6개 역할과 검수 보류 안내 확인. 유료 재생성·SNS 게시 없음.

- **새 영상은 자막과 한국어 보이스가 필수다.** 전문가 역할 팩 v3에서 스토리보드·카피·비주얼·독립 검수 지침을 강화했다. 시간별 `video_narration`을 보이스 프롬프트와 자막 합성에 함께 사용한다. 480p 약 28px 흰 글씨·검은 외곽선·어두운 배경·최대 두 줄, 하단/오른쪽 여백을 적용하며 MP4에 직접 입힌다. 수동 영상에도 보이스 대본 입력을 추가했다.
- 원본·대본·자막 완성본·해시 receipt를 보존하며 편집 실패/무음은 확인 대기로 남겨 유료 재제출 없이 편집만 복구한다. 오디오 존재와 실제 보이스 검증을 구분하고 quality에 `video_checks`가 없으면 통과시키지 않는다. 기존 접수/완료 영상은 변경하지 않는다. 실제 자막 합성·무음 거절·재시도·버전 고정 테스트를 포함해 Python 230개와 UI 11개가 통과했다. 새 유료 생성·SNS 게시는 수행하지 않았다. 운영/설치는 [영상 자막·보이스](docs/video-delivery.md)를 참고한다.

- **스토리보드 제작 기준을 역할 묶음 v4로 보강했다.** 사용자가 지정한 Instagram 4개 계정의 게시물 12개를 브라우저에서 열어 본문·이미지·재생 중 표본 프레임을 조사하고 [MD 자료 7개](docs/storyboard/README.md)를 저장했다. 전편 연속 시청·음성 청취·정확한 컷 수는 미검증이며 문서에 범위를 명시했다. 직전 영상의 저장된 설계는 20초 단일 고정 숏·10초 손수건 정리·6초 표정 마무리라 변화가 약했다.
- 기획의 `story_premise`, 스토리보드의 `story_design`·장면별 새 정보·행동 전후·시간표·도입 후보·`engagement_review`, 검수의 `story_checks`를 전문 역할 계약에 추가했다. 스토리보드 `playbook`에 [제작 지침 v1](docs/storyboard/director-playbook-v1.md) 본문·SHA-256을 넣어 기존 역할 동결 로직으로 신규 run의 단계 입력에 전달한다. 기존 보이스·자막 지침, 선택적 영상 MD의 낮은 관여도, 페르소나·비용·사람 승인 정책을 보존한다. 과거 run 재개에 소급 적용하지 않는다.
- 전문가 관련 **11개 테스트 통과**, 격리 저장소에서 MD 본문 전달·실행 후 설정 변경 시 동결 유지·예시 대사의 현재 자막 계약·문서 링크·12개 원문 URL을 확인했다. 이 변경은 지침/설정/MD만 수정했으며 실제 생성·재시도·서버 재시작·SNS 게시를 수행하지 않았다. 실제 결과의 재미 개선은 새 영상 생성 후 검증해야 한다.

- **GS 소재 수집 대상을 GS리테일(편의점,수퍼) / GSSHOP / GS건설 / GS칼텍스 / 파르나스 호텔로 재정의했다.** GS리테일은 GS25·GS THE FRESH 범위다. 설정·리서처 지침·공식 도메인 검증·수동 소재/기획 대상·대시보드·현재 문서를 함께 맞췄다. 과거 소재·페르소나·실행 스냅샷은 보존하며 기존 `GS SHOP` 저장 식별자는 화면에 `GSSHOP`으로 표시한다. 상세 범위는 [GS 소재 자동 탐색](docs/source-research.md)을 따른다.

- **자동 영상 오류 복구를 보완했다.** 외부 생성 POST 재전송 금지를 유지하면서 제출 전 참조 업로드·기존 작업 조회·다운로드에 10/20/40/80초 간격의 DB 기반 복구를 적용했다(연속 오류 5회 한도). 결과 URL/로컬 파일부터 재개하고, 만료된 다운로드 주소는 같은 외부 ID로 갱신한다. 45분 관찰 제한은 수동 조회 재개 시 새로 시작한다. 완료 영상의 제작 단계 반영은 별도 `sync_pending/sync_errors/sync_error`로 복구하며 파일과 completed 상태를 보존한다. HTTP 본문 일부 다운로드, 무한 URL 갱신, 재시작 후 재시도 초기화, 완료 영상 재생성 위험을 검증했다.
- Python 전체 **218개**, UI 로직 **11개** 테스트 통과. 격리된 브라우저 응답으로 오류·복구 버튼·영상 라이브러리 안내를 검증했고 실제 유료 요청/게시를 테스트 목적으로 만들지 않았다. 기존 영상은 생성·저장이 완료됐고, 독립 검수 런타임의 audio input unsupported 때문에 quality가 확인 대기다. 재생성은 필요하지 않으며 같은 영상의 음성 내용 확인이 남았다. 기존 총괄이 종료된 뒤 8767을 정리하고 **8765 PID 96685**의 최신 영상 작업자로 잠금을 인계했다. 활성 AI를 중단하지 않았고 기존 영상 2건·외부 ID·completed 상태를 유지했다. SQLite quick_check 정상, API 200, 영상 작업자 단일 소유를 확인했다. 배포 직전 백업은 `.runtime/before-video-recovery-20260921.sqlite3`, 배포 결과는 `.runtime/video-recovery-rollout.json`이다.


- **첫 전문가에서 멈춘 것처럼 보이던 단계 상세 창을 수정했다.** 기존 창은 최초 조회 결과만 표시하고 live 배지를 쓰지 않아 ‘배정됨’에 머물렀다. 모든 단계 창에 5초 갱신·실제 상태·최근 로그·연결 신호 구분·오류 복구를 적용했고, 닫기/전환 시 요청 취소 및 영상 재생 유지도 확인했다. 실제 `run-fdb94de359954c75`는 19:45 소재 3건 등록→기획→19:49 스토리보드 배정까지 진행했다. UI 테스트 8개 및 격리 브라우저 검증 통과. 진행 중 제작·서버·DB는 재시작하지 않았다. 자세한 사항은 `docs/production-monitor.md` 참고.

- **준비 화면 지연을 점검하고 시작 절차를 줄였다.** `run-fdb94de359954c75`는 19:36:31 요청 → 19:36:33 총괄 시작 → 19:37:36 sources 확보 → 19:38:04 실제 리서처 배정 → 19:39 공식 웹 탐색 기록이 확인됐다. 모델 실행 전 대기는 없었고 시작 후 전체 운영·과거 인계 문서를 읽는 단계가 약 1분 걸렸다.
- `runtime_bootstrap.py --run-id ID --worker-id ID`가 예상 run 검증·단계 확보·private claim(0600)·공개 입력 저장을 한 번에 처리한다. 로컬 런타임 지침의 첫 명령이며 해당 단계/전문가에게 필요한 문서만 읽도록 바꿨다. 새 helper는 별도 CLI에서 최신 코드를 읽으므로 진행 중인 서버/제작을 재시작하지 않았다. 현재 run은 기존 claim으로 계속 진행하며 새 총괄부터 짧은 시작 경로를 사용한다.
- UI는 실제 runtime 상태에 따라 연결·입력 확인·요청 후 경과 시간·2분 이상 첫 단계 미시작을 구분한다. 다른 run의 runtime을 사용하지 않는다. 전체 Python **205개**·UI 로직 **6개** 테스트 통과 및 실제 제작 현황에서 GS 소재 담당 배정·콘솔 오류 없음 확인. 이 변경에서 현재 제작을 중지하거나 별도 중복 생성 요청을 보내지 않았다.

- **일시정지·중지·재개 프로세스를 재검토했다.** 영상 제출 직전의 상태 변경 경쟁, 제출 전 큐·단계 취소 누락, 일시정지 중 작업자 종료 상태 누락, 총괄·도구 프로세스 잔류, 종료 후 늦은 재제작으로 결과 무효화, 화면의 연속 제어 요청 누락을 수정했다. 중지 이후 새 유료 전송 예약을 차단하고 이미 진행 중인 외부 영상 결과와 사용자의 중지 상태는 보존한다. 상세 원인·복구 기준은 [제어 프로세스 검토](docs/control-review-2026-09-21.md)에 기록했다.
- 전체 Python **205개**·화면 로직 **8개** 테스트와 격리 브라우저의 종료 시각 없는 시작→일시정지→재개→중지를 확인했다. 실제 제작 실행을 임의로 시작·재개하거나 유료 미디어·SNS를 호출하지 않았다.
- 검토 도중 사용자가 새 영상만 실행 `run-fdb94de359954c75`(v3·목표 1건·종료 시각 없음)를 시작했다. 해당 총괄은 유지하고 기본 서버 8765를 갱신했다. SQLite 취소 상태 보호도 적용하여 기존 작업자의 늦은 상태 변경을 차단한다. 확인 시 8765·8767 모두 상태 API 200이며 새 실행의 설정·running 상태를 보존했다. 아래 서버 한 개·현재 중지 기록은 이전 시점의 이력이다.

- **2026-09-21 19:22 서버 정리·재기동:** 같은 프로젝트 DB를 사용하던 8765·8767 서버를 정상 종료하고 최신 서버를 **8765 하나로 통합**했다. 기본 주소는 http://127.0.0.1:8765 이고 8767은 닫혀 있다. PID는 `.runtime/server.pid`, 표준 출력·오류 로그는 `.runtime/server.log`에 보존한다. 로컬 제작·영상 작업자 잠금은 같은 서버가 보유하며 HTTP/주요 API와 SQLite quick_check가 정상이다. 사용자 중지 run(end_at=null)과 콘텐츠·버전·제작·영상 레코드는 보존했다. 재기동 직전 DB 백업은 `.runtime/before-server-restart-20260921-192150.sqlite3`다. 자동 제작을 임의로 재개하지 않았다.

- **중지 시 ISO 날짜 오류 복구:** 종료 제한 해제 이후 8767 서버만 이전 Python 코드를 실행하여 `end_at=null`인 실행의 상태 조회·중지에서 400을 반환했다. 8765의 최신 API로 사용자가 요청한 `run-4da016a9d6bb48ba` 중지를 완료하고 8767을 최신 코드로 재시작했다. 두 포트 모두 `/api/state` 200·stopped·end_at=null을 확인했다. 임의 종료 시각을 넣거나 새 제작을 시작하지 않았다. 같은 DB를 쓰는 여러 서버가 있으면 백엔드 변경 시 모든 서버의 적용 여부를 확인한다. 아래 과거 running 기록보다 현재 DB 상태를 우선한다.

- **시작 버튼을 로컬 Codex 제작 작업자에 직접 연결했다.** `serve`에서 전용 총괄을 실행하고 시작·재개·기획안 요청 직후 깨운다. 다른 개발 대화의 5분 heartbeat에 의존하지 않는다. 기존 ChatGPT 로그인과 기본 모델을 사용하며 DB별 잠금·단계 소유권으로 중복을 막는다. 실행 실패는 확인 대기와 사유로 표시한다. 구현·비공개 진단·운영 계약은 [로컬 제작 작업자](docs/local-production-worker.md)에 있다. 아래 heartbeat 방식 설명은 이전 이력이다. 격리 HTTP 시작→작업자 프로세스→첫 단계 완료와 중복·중지·오류 보호를 포함한 Python 195개/Node 5개 테스트를 통과했다. 실제 Codex CLI도 별도 테스트 DB에서 첫 단계 저장을 확인했다. 운영 실행은 사용자 중지 상태를 보존했고 이번 점검에서 유료 미디어 생성·SNS 게시는 수행하지 않았다.

- **오전 9시 자동 생성 마감을 해제했다.** 기본 `generation_end_at`은 null이며 종료 시각을 비운 새 실행에 임의의 8시간 제한을 넣지 않는다. 실행별 `end_at=null`은 시간 제한 없음이고 목표 건수·사용자 중지·비용/오류 한도는 유지한다. `POST /api/runs/:id/deadline {"end_at":null}`로 진행 중인 실행의 작업·소유권·결과를 보존하며 마감만 해제할 수 있다. 기존 SQLite의 end_at NOT NULL 제약은 레코드·인덱스를 보존해 이전한다.
- 각 단계 완료 결과는 즉시 DB·단계 파일·로그에 저장하고 다음 단계를 진행한다. UI 상태는 5초(영상 라이브러리 3초)마다 갱신한다. 과거 09:00 생성 마감이나 12:00 후속 확인 종료 문구로 새 작업을 지연하거나 임의 종료하지 않는다. 게시 승인·계정 연결·별도 게시 시각 조건은 생성 완료와 다른 계약이다.
- 적용 확인: 현재 `run-4da016a9d6bb48ba`는 end_at=null, running, 목표 1건을 유지한다. 변경 전후 cycle·job 전체 레코드와 소유권이 동일함을 확인했다. 서버 8765를 재시작해 반영했고 백업은 `.runtime/before-no-deadline-20260921.sqlite3`이다. 전체 Python 184개·UI 로직 5개 테스트와 실제 제작 현황 ‘시간 제한 없음’ 표시, 콘솔 오류 없음 확인. 이 변경은 실제 생성·게시 호출을 추가하지 않았다.

- **콘텐츠 제작 → AI 자동 제작에 ‘영상만’을 추가했다.** `media_mode: video_only`로 저장하며 기존 이미지+짧은 영상·이미지 중심 선택은 유지한다. 기획은 video 형식, 제작 완료·등록은 MP4만 허용하며 등록된 페르소나 사진·영상 길이·해상도·MD를 기존 영상 대기열에 전달한다. API 키·사진·비용·대기 한도 미충족은 이미지로 대체하지 않고 확인 대기로 저장한다. 조건 해결 후 사용자가 재개하면 같은 미디어 단계에서 재시도 횟수 추가 없이 이어간다. 과거 `video` 모드는 영상 우선 동작을 유지한다.
- 이 변경에서 전체 **177개 테스트 통과**. 격리 UI에서 영상만 선택→25초·480p 실행 저장→새로고침 후 선택 유지와 잠금을 확인했다. 실제 진행 중인 실행의 상태·설정은 보존했으며 유료 영상 생성·SNS 게시를 새로 호출하지 않았다. 서버 재시작을 완료했고 기존 실행의 형식은 변경하지 않는다.

- **전역 topbar 상태 버튼과 별도 제작 현황 화면(`/#production`)을 추가했다.** active run(예약·제작·일시정지·확인 필요)이 있을 때 상태·단계·완료/목표 수를 표시한다. 수정 중인 문안이나 재생 중인 영상을 다시 만들지 않고 상단 상태를 독립 갱신한다. 5초 갱신과 8초 조회 타임아웃·연결 오류 표시를 적용했다. 완료·중지 후 버튼은 숨겨지고 사이드바에서 기록을 열 수 있다.
- 제작 현황에서 run에 고정된 페르소나·전문가 구성·현재 콘텐츠의 단계·결과·로그·종료 사유와 실행 제어를 제공한다. 불확실한 결과는 확인 전에 재개 버튼을 활성화하지 않는다. 기존 정적 자산 app.js/style.css에 구현하여 서버 재시작이 필요하지 않다.
- 최신 UI 작업 도중 사용자가 자동 제작 실행을 시작하고 변경하는 것을 읽기 조회로 확인했다. 과거 ‘현재 중지’ 문구보다 실제 저장 run 상태를 우선한다. 이 UI 작업은 실제 실행을 중지·재개하거나 새로 생성하지 않았다. 유료 생성과 실제 게시도 호출하지 않았다.

- **등록된 페르소나 사진을 영상의 필수 인물 기준으로 고정했다.** 수동·자동 영상 모두 해당 버전의 사진을 첫 참조로 보내고 연령·외형·메이크업 지침과 사진 해시를 입력에 저장한다. 수동 추가 업로드는 보조 장면 참조이며 등록 사진이 있으면 사진 없는 시안 요청을 화면·API에서 차단한다. 프로필 후속 변경·서버 재시작에도 접수한 참조를 유지한다. 전체 **170개 테스트 통과**. 새 유료 영상 생성은 수행하지 않았으며 실제 생성 결과의 얼굴 일관성 검수는 별도다.

- **자동 제작 개수를 선택할 수 있다.** 콘텐츠 개수 ‘제한 없이’ 또는 ‘개수 지정(1~1,000)’은 새 실행의 `settings.stop_after_posts`에 고정된다. 성공한 피드 등록만 세고 목표 또는 종료 시각 중 먼저 도달하면 완료한다. 사람 승인, 카드 장수, 실패·재시도는 개수에 포함하지 않는다. 이 항목은 아래 이력의 ‘총량 제한 없음’ 설명보다 우선하며, 기본값만 제한 없음으로 유지한다.
- 목표 진행률과 완료 사유를 화면·로그에 표시한다. 실행 중 개수 변경은 거절하고 새 실행에서 변경한다. heartbeat 운영 지침도 저장된 목표와 종료 상태를 따르도록 갱신했다.
- 전체 로직 점검 중 재작업의 오래된 하위 입력·결과·담당 표시, 영상 수정 요청 키 충돌과 늦은 콜백, 만료 콘텐츠의 게시 대기열 차단, 잘못된 출처 입력 오류를 수정했다. **전체 167개 테스트 통과**, 격리 대시보드의 2건 정확 종료·무제한·재개·모바일을 검증했다. 유료 생성·실제 게시·운영 실행 재개는 하지 않았다. 상세 결과는 [로직 점검 기록](docs/logic-review-2026-09-21.md)을 참고한다.

- **최신 외형은 프로필 v3:** 사용자가 v2의 화장이 강하다며 **수수하고 청순한 내추럴 분위기·연한 화장**을 요청했다. 30대 중반 설정은 유지했다. Segmind `seedream-4` 최종 사진은 `output/persona/yeouido-mokdong-mom/segmind-reference-v7/mother-reference.jpg`, SHA-256은 `04baf41d138b2606f0af8437a4024bf1d25311b08f6357f9c4a7abba6bac405a`다. v7는 후보 번호이며 프로필은 v3다. 최신 `appearance`에 얇은 베이스·자연스러운 눈썹·은은한 눈매·연한 피치 핑크 립과 강한 색조·글로스를 피하는 기준을 저장했다. v1·v2 참조와 기존 실행 스냅샷을 보존했고 새 자동 실행부터 v3를 사용한다. 이번 수정 비용은 후보 2건 합계 $0.07다. 8765·8767 서버의 새 버전·이미지 해시를 확인했으며 기존 실행은 중지 상태다. 실제 영상의 얼굴 일관성은 이후 검수 대상이다.

- **AI 자동 제작을 제작 총괄 + 7개 전문 서브에이전트로 구성했다.** 역할은 소재 리서처, 라이프스타일 기획자, 스토리보드 디렉터, 페르소나 카피라이터, 비주얼 프로듀서, 독립 품질 검수자, 피드 패키징 에디터다. `config/specialists.json`에 전문성·행동 지침·출력 계약을 저장하며 새 run에 버전을 고정한다. 기존 중지 run은 그대로 유지한다.
- Codex heartbeat가 실제 `collaboration.spawn_agent`/`followup_task`로 위임하고 `worker-agent`/`POST /api/worker/agent`로 실제 canonical agent ID를 기록한다. 총괄이 lease를 유지하고 전문가 결과만 검증·저장한다. 새 run은 해당 단계·시도의 `specialist_assignment_id` 없이 완료할 수 없으며 제작 agent는 같은 콘텐츠의 quality 담당을 겸할 수 없다. 웹 소재 등록과 비동기 Segmind 완료도 원래 위임 기록에 연결된다. 담당자·중단·실패·인계 이력은 단계 결과와 로그에 보존된다.
- AI 자동 제작 화면의 전문가 팀은 설정된 역할과 실제 배정을 구분한다. 기존 run의 동결 설정과 다음 run의 configured_roles도 구분한다. 실제 agent ID는 총괄이 런타임에서 보고하는 값이며 서버가 Codex 내부 실행을 독립 인증하지는 않는다. 역할만 등록된 상태를 실행 완료로 표시하지 않는다. 운영 방식과 기존 heartbeat 프롬프트는 [전문가 위임 절차](docs/specialist-agents.md)로 갱신했다.
- 전문가 변경 후 전체 **146개 테스트 통과**. 별도 격리 run에서 실제 `/root/persona_copywriter_check` 서브에이전트를 생성·배정하고 결과 JSON을 반환받아 HTTP 완료→다음 단계 인계를 검증했다. 이 검증의 선행 3단계는 명시적인 합성 fixture이며 실제 제작으로 세지 않는다. UI의 역할·실제 담당·인계 요약과 390px 모바일을 확인했다. 이 검증에서 유료 미디어 생성과 SNS 게시는 하지 않았고 실제 run의 사용자 중지 상태·기존 영상 1건·영상 MD 4개를 보존한다.

- **직전 v2 외형 이력:** 사용자가 v1 사진이 나이 들어 보이고 푸근하다며 **30대 중반·세련된 인플루언서 분위기·적절한 메이크업**을 요청했다. Segmind `seedream-4`로 새 사진을 제작하고 DB 프로필의 연령과 `appearance` 지침을 수정했다. 최종 사진은 `output/persona/yeouido-mokdong-mom/segmind-reference-v5/mother-reference.jpg`, SHA-256은 `fb39a6c3116596692cfdfb6c79a60c9f0fa480ad6309c858a15a4a1780445e6b`다. v5는 후보 번호이며 프로필은 v2다. v1 참조·기존 콘텐츠·실행 스냅샷은 보존했고, 새 자동 실행부터 v2 사진을 사용한다. 이번 수정의 공급자 보고 비용은 후보 2건 합계 $0.07다. 현재 페르소나 기준은 DB v2 및 `docs/persona.md`이며 초기 config의 30대 후반 설정보다 우선한다. 기존 실행은 중지 상태다.
- **직전 v1 외형 등록 이력:** 2026-09-21 사용자가 Segmind로 친근한 인물 한 명을 만들고 대시보드에 등록하라고 요청했다. 최종 `seedream-4` 사진을 `yeouido-mokdong-mom` 프로필 v1의 `mother` 참조에 고정했다. 원본은 `output/persona/yeouido-mokdong-mom/segmind-reference-v3/mother-reference.jpg`, SHA-256은 `0b596bf573a2e993058f23788059d9b39101c9920d5a93cff3fc2e132c61641c`다. v3는 이미지 후보 번호다. 앞선 FLUX 후보 2장은 미채택했으며 모든 요청·외부 ID·결과·프롬프트·검수를 해당 폴더에 보존했다. 당시 총 공급자 보고 비용은 $0.25775다. 실제 영상의 얼굴 일관성은 아직 검증하지 않았으며, 딸의 기준 이미지와 활동명은 미정이다.

- **AI 자동 제작 첫 단계에 GS 소재 웹 탐색·자동 등록을 연결했다.** 새 run은 `source_research_enabled:true`이며 Codex 작업자가 공식 원문을 읽고 `worker-sources`/`POST /api/worker/sources`로 조사 기록과 소재를 원자적으로 저장한다. 실제 웹 검색은 heartbeat가 수행하며 서버는 검색 엔진이 아니다. 기존 `boca-10` 프롬프트와 [조사 운영 절차](docs/source-research.md)도 함께 갱신했다. 버튼 요청 후 다음 작업자 확인까지 약 5분이 걸릴 수 있다.
- `source_research_receipts`에 검색어·본문 근거·채택/재사용/제외 사유를 보존한다. 공식 도메인, 최근 확인일, 행사 유효기간, 단계 소유권, 소재 버전을 검증한다. URL 추적 인자를 정리해 중복을 줄이고 사람이 수정한 소재는 자동 결과로 덮어쓰지 않는다. 새 실행은 실제 조사 기록 없이 sources를 완료할 수 없으며 planning은 해당 조사 소재만 사용한다. 다음 순환에는 기존 소재와 최근 사용 주제를 전달한다.
- GS 소재 관리에서 자동/수동 구분, 수집 결과·검색어·근거·제외 사유를 읽을 수 있다. 단계 결과와 LOG/JSONL에도 남으며 갱신 중 펼쳐 둔 조사 기록은 유지한다. 기존 영상 화면·MD 설정을 보존했다.
- 이 변경 후 전체 **136개 테스트 통과**. 실제 공식 페이지에서 확인한 소재 2건을 **격리 저장소**의 HTTP API로 등록하고 버튼→작업자 sources→등록→planning 이동, 화면 자동 갱신, 로그·스냅샷, 390px 모바일을 검증했다. 실제 저장소에는 테스트 소재나 새 제작 실행을 만들지 않았으며 기존 사용자 중지 상태를 유지한다. 유료 미디어 생성·SNS 게시는 실행하지 않았다.
- **영상 라이브러리 `/#videos`**를 별도 메뉴로 추가했다. `GET /api/video-library`는 현재 run·최근 50개 제한과 무관하게 전체 영상 작업 및 작업에 연결되지 않은 콘텐츠 영상을 조회한다. 자동 영상은 등록 전 실제 MP4가 저장되어도 재생할 수 있으며 과거 영상·문안 버전을 유지한다.
- 라이브러리는 3초마다 로컬 상태를 조회하고 창 복귀·진입 시 즉시 확인한다. ID·미디어 SHA-256가 같은 동안 video DOM을 유지하여 다른 작업의 상태 변경·완료·새 영상 추가·문안 수정·수동 갱신이 현재 재생을 중단하지 않는다. 검색·필터·선택 복원·파일 다운로드·연결 실패 및 복구 표시를 제공한다.
- 라이브러리 추가 후 전체 120개 테스트가 통과했다. 격리 브라우저에서 제작 상태 변경, 신규 기록 추가, 완료 후 플레이어 등장, 재생 위치·음량 보존, 선택 복원, 필터·검색, 연결 실패·복구, 390px 모바일을 검증했다. 실제 라이브러리에는 기존 ‘하루 일상 브이로그’ 1건이 표시되며 재생과 콘솔 오류 없음도 확인했다. 실제 자동 실행의 중지 상태와 콘텐츠 버전은 유지했다.
- 콘텐츠 제작에 **제작 결과** 갤러리를 추가했다. 저장된 이미지·영상·문안을 보여주며 자동 제작의 단계 기록은 기획·장면·문안·검수 결과를 읽기 쉬운 형태로 표시한다. 원본 입력·JSON과 다운로드도 유지한다. 단계의 실제 결과·체크포인트에 연결된 미디어는 피드 등록 전에도 볼 수 있으며 register 미리보기는 당시의 정확한 콘텐츠 버전에 연결한다.
- 단계 미디어 API는 `GET /api/runs/:id/stages/:cycle/:stage/media/:index`다. 프로젝트 내부의 기록된 이미지·MP4만 제공하고 영상 Range 재생을 지원한다. 임의 경로·외부 파일·심볼릭 링크 이탈을 차단한다. 미리보기 변경 후 전체 109개 테스트가 통과했다. 기존 생성 파일로 검증하며 새 유료 생성이나 SNS 게시를 시작하지 않는다.
- 브라우저에서 7단계 내용 조회, 중간 이미지 원본, MP4 재생, 결과 새로고침, 390px 모바일을 확인했다. 현재 실제 저장된 `하루 일상 브이로그` 영상은 20.064초로 재생된다. 실제 자동 run은 중지·cycle 0이며 검증용 단계 데이터를 실 DB에 넣지 않았다. 이번 미리보기 개선은 별도 콘텐츠 생성이나 승인·게시가 아니다.
- 콘텐츠 자동 생성은 AI가 일상 기획 → 스토리보드 → 게시 문안 → 이미지·선택 영상 → 품질 검수 → 피드 등록 → 다음 이야기까지 수행하는 연속 루프다. 10건 상한은 없으며 사람의 게시 승인 대기가 다음 콘텐츠 제작을 막지 않는다.
- AI 역할은 이 대화의 Codex heartbeat가 담당한다. 로컬 서버만 실행한 상태에서 LLM이 기획을 작성하는 구조는 아니다. 대시보드 버튼은 실제 실행 상태를 저장하고, Codex는 저장된 단계·입력·중간 결과에서 이어간다. 개발 자동화용 랄프 루프와 콘텐츠 운영 루프를 구분한다.
- **사용자가 기존 실행을 중지했다. 중지 상태를 보존하고 새 실행을 임의로 시작하지 않는다.** 다시 진행하려면 대시보드 콘텐츠 제작 → 자동 생성 버튼으로 시작해야 한다. 과거 17:30 예약 문구가 중지를 취소하는 권한은 아니다.
- 새 실행의 media_mode는 images/mixed/video이며 시작한 run에 고정된다. 이번 UI 기본값은 사용자가 승인한 mixed다. 기존 실행에 다른 형식을 명시하면 409로 안내하며 침묵 전환하지 않는다. API에서 형식을 생략하면 config 값 또는 images를 사용한다.
- 사용자는 Segmind 자동 영상을 기존 **한 편 예상 $8·UTC 하루 예상 $15·대기/진행/확인 필요 2건** 한도 안에서 사용하는 데 동의했다. 이 승인은 추가 크레딧 구매·한도 증액·다른 유료 공급자 사용을 뜻하지 않는다. 자동·수동 영상은 같은 한도 집계를 사용한다.
- `worker-video CYCLE TOKEN` 또는 `POST /api/worker/video`는 AI가 작성한 planning/storyboard/copy와 해당 run의 고정된 페르소나 버전을 Segmind 비동기 작업자에 전달한다. API 키·기준 이미지·비용·대기 한도 조건 미충족은 skipped/fallback:images로 기록하고 이미지 피드 제작을 계속한다. 제출 후 불확실한 작업을 자동 재제출하지 않는다.
- 자동 영상이 완료되면 실제 MP4를 저장하고 images 단계까지만 완료한다. Codex가 quality를 수행한 뒤 register에서 콘텐츠를 import한다. 수동 `/video` 화면은 유지한다. 어느 경로에서도 AI 검수는 사람의 승인과 같지 않으며 Instagram 연결·게시를 대신하지 않는다.
- 상세 파일은 `.runtime/production/<run_id>/events.log`, events.jsonl, cycles/<cycle_id>/<stage>.json에 저장한다. 입력·출력·체크포인트·시도·외부 ID·공개 결정 요약을 포함한다. SQLite 커밋 후 원자적으로 교체하고 재시작 시 복구한다. rollback 이벤트·중복 append·키/소유권 토큰 노출을 막는다. 실시간 추적은 tail -F를 사용한다.
- 파일 API는 `GET /api/runs/:id/artifacts`, `/api/runs/:id/logs?format=log|jsonl`, `/api/runs/:id/stages/:cycle/:stage`다. 모든 AI 단계는 decision_summary로 공개 선택 근거를 남긴다. 내부 사고 과정은 요구하거나 파일로 내보내지 않는다.
- 전체 102개 테스트가 통과했다. 마지막 요청 프롬프트 기록 보완 후 자동 영상 관련 11개 테스트도 다시 통과했다. 검증은 격리 DB·API 대역·테스트 미디어로 수행했으며 상세 파일의 7개 단계 조회·LOG/JSONL 다운로드 API도 확인했다. 이번 확장 중 실제 유료 생성이나 SNS 게시를 새로 실행하지 않았다.
- 격리 브라우저에서 단계 입력·출력·판단 요약 모달, LOG/JSONL/단계 JSON 다운로드, 재개·중지·새 mixed 즉시 시작·중지를 확인했다. Python 3.9 호환 시간 형식을 적용했고 JS 문법·콘솔 오류·390px 모바일 가로 넘침 검증을 통과했다. 실제 운영 실행의 중지 상태는 유지했다.

운영 절차는 [콘텐츠 작업자 실행](docs/overnight-runbook.md), [API](docs/local-api.md), [Segmind 자동·수동 영상](docs/segmind-video.md)을 따른다.

## 직전 인계 이력 — 종합 스튜디오 구현 이후

이 절은 종합 스튜디오가 완성된 시점의 기록이다. 화면·버전 보호 구조는 유지하며 현재 실행 상태·자동 영상 범위·로그 기능은 위 최신 인계를 따른다.

- 대시보드는 **개요·페르소나·콘텐츠 제작·검수 & 승인·피드 미리보기·GS 소재 관리·활동 로그·설정의 8개 화면**으로 확장했다. 자동 생성은 콘텐츠 제작 메뉴의 탭이다. 예약 제어가 첫 화면 전체를 차지하던 구성은 교체했다.
- 여러 페르소나의 생성·조회·버전 수정·작업 대상 선택, 기획안의 생성·조회·수정·제작 요청을 구현했다. 삭제 기능까지 구현됐다는 뜻은 아니다. 기획안은 실제 제작 큐와 단계 작업 입력으로 전달된다.
- 수동 제작은 요청한 기획 대기열을 처리한 뒤 종료한다. 콘텐츠 자동 생성은 명시적으로 시작하며 **10건 상한 없이** 종료 시각 또는 중지 조건까지 순환한다. 승인 대기는 다음 콘텐츠 생성을 막지 않는다.
- 기존 야간 생성 창은 **2026-09-21 17:30~2026-09-22 09:00 KST**다. Codex heartbeat는 **5분마다** 저장된 실행 상태를 확인한다. 대시보드 실행 상태와 일시정지·중지 요청을 우선하며, heartbeat 확인과 실제 외부 생성 완료는 구분한다.
- Python 표준 라이브러리 백엔드·SQLite·로컬 미디어 저장소와 HTML/CSS/JavaScript 대시보드가 구현되어 있다. 기본 대시보드 주소는 http://127.0.0.1:8765 이다. 실제 서버 실행 상태는 접속 시 확인한다.
- 실행은 시작 시점의 페르소나 버전을 유지한다. 사람의 콘텐츠 수정·승인본을 보호하며 기획 중지·기한 종료 후 수정·재제작을 지원한다. 엄마·딸의 외형 참조는 페르소나 버전별 불변 파일과 해시로 보관하고 최초 등록을 해당 실행에 반영한다.
- **종합 스튜디오 범위의 전체 66개 테스트가 통과했다.** 격리된 서버에서 페르소나 생성·전환, 기획 저장·제작 요청, 자동 생성 제어, 검수 수정·승인·재승인·ZIP 다운로드, 소재·설정 저장을 확인했다. 데스크톱·모바일 8개 화면의 배치도 확인했다. 이는 실제 운영 데이터로 이미지·영상을 생성하거나 SNS 게시한 검증이 아니다. 별도 병행 작업이 아래에 남긴 테스트 수와 실행 결과는 그 작업의 기록으로 구분한다.
- 실제 Instagram OAuth·게시 연결은 완료되지 않았다. 내부 페르소나나 대상 계정 아이디 저장으로 Instagram 가입·인증·프로필 변경이 수행되지 않는다. 실제 게시에는 사람의 정확한 버전 승인과 실제 대상 계정·게시 도구 확인이 필요하다.
- 내장 이미지 도구로 사람이 없는 생활 공간 샘플을 만든 기록은 있지만, 첫 페르소나 외형 생성과 여러 피드의 일관성·야간 무인 생성 전체 과정은 별도 확인이 필요하다. 병행 영상 작업의 세부 구현·인증·생성 결과는 해당 작업 기록과 실제 환경에서 확인하며 여기서 추정하지 않는다.

다음 작업자는 [기능 범위](docs/dashboard-product-review.md), [UI 설계](docs/review-ui-design.md), [API](docs/local-api.md)를 읽고 실제 저장 상태를 확인한다. 이미 답한 Q6~Q8이나 폐기한 MD-EYE 배경을 다시 묻지 않는다.

## 과거 기록을 읽는 방법

아래 내용은 결정·구현 과정을 보존한 이력이다. 과거 문단 안의 ‘현재’, ‘최신’, ‘아직’, ‘구현 전’은 **그 문단을 작성한 당시**를 뜻한다. 오늘의 현황이나 새 작업 지시로 적용하지 않는다.

| 과거 기록의 표현 | 현재 적용 기준 |
| --- | --- |
| 08:00 임시 종료 | 2026-09-22 09:00 KST 생성 종료 |
| 매시 30분 후속 실행 | 5분 간격 heartbeat로 저장된 실행 상태 확인 |
| 10건 제작 후 종료 | 10건 상한 없는 연속 모드와 별도 수동 기획 모드 |
| 13개·38개 테스트 통과 | 종합 스튜디오 범위 66개 테스트 통과; 별도 병행 작업 결과는 해당 기록 참조 |
| 화면 재구현 요청이 아님 | 이후 사용자가 종합 대시보드 재구현을 요청했고 8개 화면으로 구현 |
| 빈 폴더·구현 전·단일 프로필·페르소나 생성 미구현 | 백엔드·DB·8개 화면·다중 페르소나·기획 큐 구현 |
| Q6~Q8 답변 대기 | 완료 범위, 워킹맘·다섯 살 딸, 일상 먼저·GS 소재 연결 원칙에 대한 답변 반영 |

## 연결 이력 — GitHub, 2026-09-21

이 항목은 연결을 수행한 시점의 관찰 기록이다. 커밋·push 여부 등 이후 상태는 실제 저장소를 확인한다.

- 사용자 지정 협업 저장소: https://github.com/learnerunick/2026gshackathone.git
- 기존 프로젝트 파일을 보존한 상태로 로컬 Git 저장소를 초기화했다. 초기 브랜치는 main이며 origin을 위 주소로 연결했다.
- 원격 조회·fetch에 성공했다. 확인 시점의 원격 저장소에는 커밋·브랜치가 없었다.
- 아직 로컬 커밋·원격 push는 수행하지 않았다. 원격 쓰기 권한도 검증 전이다.
- 런타임 DB·로컬 인증정보·생성 미디어·브라우저 검증 기록은 기존 .gitignore로 제외한다.

## 과거 결정 이력 — 대시보드 기능 재검토 단계

이 단계 이후 종합 대시보드 재구현 요청과 실제 구현이 이어졌다. 아래 ‘재구현을 요청한 상태는 아니다’는 당시 상황만 기록한다.

- 사용자가 대시보드의 중심을 페르소나 생성과 계정별 일상 피드 운영으로 다시 정의했다. 기능 검토안은 docs/dashboard-product-review.md에 있다. 화면의 재구현을 요청한 상태는 아니다.
- 주요 소재 범위는 GS SHOP 중심에서 GS칼텍스·GS건설·GS리테일·GS SHOP으로 확장됐다. Instagram의 표현은 일상 로그를 유지한다.
- 콘텐츠 생성 종료 시각을 2026-09-22 09:00 KST로 확정했다. 08:00이라는 이전 임시값보다 이 결정이 우선한다.
- 최소 10건, 유동적인 카드 수, 추가 AI API 결제 금지, 사람 승인 후 게시 조건은 유지한다. 기존 데모 계정 @아이디는 아직 미확정이다.

## 과거 구현 이력 — 2026-09-21 16시대, 초기 야간 제작 준비

이 항목은 최초 구현 당시의 기록이다. 아래의 08:00 임시값·매시 30분 예약·13개 테스트·간단한 검토 화면은 이후 갱신되었으며, 맨 앞의 현재 요약을 적용한다.

- 사용자가 MVP B(실제 생성 → 대시보드 수정·승인)를 선택했으며, 다음 날 아침 승인한 콘텐츠의 실제 Instagram 게시도 요청했다.
- 2026-09-21 17:30부터 밤사이 최소 10건의 피드를 준비한다. 다음 날 종료 시각은 별도 답변이 없어 08:00으로 임시 설정했다.
- 페르소나: 한국인 여성, 30대 후반, 서울 여의도에서 일하는 워킹맘, 목동 거주, 집 가꾸기 선호, TJ 같은 성격. 엄마를 닮은 다섯 살 외동딸이 가끔 등장한다. 이름·얼굴 기준 이미지는 아직 미정이다.
- GS SHOP에서 판매하는 상품을 우선하며 일상 이야기를 먼저 만든다. GS SHOP 판매처명·로고는 공개 콘텐츠에 넣지 않는다. 상품 출처는 내부 자료로 유지하고 가상 인물 표시는 생략하지 않는다.
- 추가 API 결제는 불가하다. Codex CLI의 ChatGPT 로그인 상태와 내장 이미지 생성 도구 존재를 확인했고, 기존 테스트 이미지 생성 성공 기록이 있다. 예약 실행에서 내장 도구가 실제 동작하는지는 첫 실행에서 확인해야 한다.
- 로컬 Python 표준 라이브러리 + SQLite 검토 API와 간단한 웹 검토 화면을 구현했다. http://127.0.0.1:8765 에서 제목·문안·해시태그 수정, 버전별 승인·반려, 생성 요청 큐, 작업 상태 확인을 지원한다. 내장 이미지 도구는 이 API가 직접 호출하지 않고 Codex 예약 작업이 호출해 결과를 등록한다.
- 늦은 AI 결과의 별도 제안 보관, 수정 후 재승인, 동일 결과 중복 등록 방지, 미디어 해시 검증, 작업별 재시도 한도, 게시 작업 단일 획득 및 불확실한 게시 재시도 금지를 구현했다. 핵심 상태 전환 테스트 13개가 통과했다.
- 이미지 순서 편집 UI, AI 제안 적용 UI, Instagram OAuth 앱·토큰·직접 API 게시 워커는 아직 구현하지 않았다. 계정 로그인·실제 Instagram 게시도 검증 전이다.
- 사용자는 기존 데모 계정을 사용하기로 했다. @아이디는 아직 받지 않았다. 게시 계정 연결 전에는 승인 결과를 대기 상태로 보관한다.
- Codex의 이 대화에 연결된 예약 boca-10을 생성했다. 오늘 17:30부터 매시 30분에 후속 작업하며, 다음 날 12:00에 종료하도록 저장했다. 예약 등록 성공은 10건 제작 완료나 실제 게시 성공을 의미하지 않는다.
- 실행 지침: docs/overnight-runbook.md. 설정: config/persona.json, config/overnight.json. 이야기 초안: config/content-plan.json. API 명세: docs/local-api.md.

추가 업데이트: 같은 날 추천 스킬 설치와 프로젝트 전용 `persona-content` 작성을 완료했다. [스킬 연결 내역](SKILLS.md)에 출처·설치 범위·검증 결과를 기록했다.

### 과거 확인 이력 — 2026-09-21, 최초 이미지 생성 경로 확인

아래 ‘등록 방식은 구현 전’과 ‘애플리케이션 구현은 진행하지 않았다’는 이 확인 작업 시점의 기록이다. 이후 결과 등록 API와 종합 스튜디오가 구현되었다.

- **추가 결제 없이 현재 지급된 계정으로만 진행한다.** 사용자가 확정한 제약이다. 아래의 API 예산 미정 및 유료 API 실행 전이라는 과거 기록보다 이 업데이트를 우선한다.
- 사용자가 제공한 OpenAI API 키로 모델 목록 조회에 성공했다(HTTP 200, 모델 124개). 키 값은 이 문서나 프로젝트 파일에 저장하지 않았다.
- 사용자의 이미지 생성 요청에 따라 Image API를 호출했지만 `429 / credit_balance_exhausted`로 실패했다. 이 경로에서는 이미지가 생성되지 않았다.
- 이후 **Codex 내장 이미지 생성 도구로 생활 공간 테스트 이미지 1장 생성에 성공**했다. 별도 API 키나 크레딧 충전을 사용하지 않았다. 현재 계정의 사용 한도 내에서 진행하며, 무제한 사용을 보장하는 것은 아니다.
- 생성 파일: [생활 공간 테스트 이미지](output/imagegen/lifestyle-builtin-test-20260921.png), [실제 사용 프롬프트](output/imagegen/lifestyle-builtin-test-20260921.prompt.txt).
- 이 이미지는 사람·브랜드가 없는 기능 확인용 샘플이다. 페르소나 외형 확정이나 인물 일관성 검증은 아직 하지 않았다.
- 현재 제안하는 MVP 경로는 ‘Codex에서 콘텐츠·이미지 제작 → 파일 또는 결과 묶음을 대시보드에 등록 → 수정·승인’이다. 등록 방식은 구현 전이며, 내장 이미지 도구를 대시보드 백엔드에서 직접 호출할 수 있다고 검증한 것은 아니다.
- 추가 유료 API 호출, 실제 SNS 게시, 애플리케이션 구현은 이 업데이트에서 진행하지 않았다.

이 문서는 기존 웹 대화에서 전달된 프로젝트 배경과 이후 Codex 대화에서 확인·결정한 내용을 정리한다. **사용자가 확정한 내용, AI가 제안한 내용, 아직 결정하지 않은 내용을 구분**한다. 다른 팀원이나 AI가 배경 설명을 처음부터 다시 요구하지 않고 작업을 이어가는 것이 목적이다.

## 1. 프로젝트 개요

- 2026 GS그룹 해커톤 ‘랄프톤’에 참여하는 4인 팀이다.
- 팀은 프론트엔드, 백엔드, AI·데이터, 기획·현업 역량을 보유하고 있다.
- 최초 요청자는 GS SHOP 웹사이트 몰의 상품·전시 업무를 담당한다.
- 서비스는 GS SHOP뿐 아니라 다양한 GS 계열사의 브랜드·상품을 다룬다.
- 사용할 데이터는 공개 자료 또는 가상 데이터다.
- 이전에 검토했던 전시 오류 감시 아이디어 **MD-EYE는 폐기했다.**

만들려는 서비스는 **가상 인플루언서의 콘텐츠를 반복 생성하고, 사람이 대시보드에서 검토·수정·승인한 콘텐츠만 SNS에 게시하는 시스템**이다. GS 계열사의 브랜드 가치나 상품 소식을 캐릭터의 일상 이야기에 자연스럽게 연결하려 한다.

## 2. 과거 초기 조사 — 당시 실제 작업 현황

프로젝트를 최초로 인계받았을 때 확인한 상태를 보존한다. 다음 표와 완료 범위는 현재 현황이 아니다.

| 항목 | 확인된 상태 |
| --- | --- |
| 프로젝트 폴더 | `boca/`; 이 문서 작성 전에는 숨김 파일을 포함해 빈 폴더였음 |
| Git | 초기화되지 않았으며 Git 저장소가 아님 |
| 기존 코드·설정·문서 | 없음 |
| 적용할 `AGENTS.md` | 초기 조사에서 현재 폴더 및 상위 경로에 없음 |
| 프론트엔드·백엔드·워커 | 구현 전 |
| DB·미디어 저장소 | 선택 및 구축 전 |
| GitHub 협업 저장소 | 이 대화에서 생성하지 않음 |
| AWS·배포 환경 | 생성하거나 배포하지 않음 |
| 콘텐츠 생성 | 실제 텍스트·이미지·영상 생성 파이프라인 실행 전 |
| SNS | 계정 연결 및 실제 게시 전 |

완료한 작업은 **폴더 현황 조사, 요구사항 정리, 설계 방향에 대한 질의응답, `grill-me`와 추천 스킬 설치·연결, 프로젝트 전용 `persona-content` 작성, 인계 문서 작성**이다. 애플리케이션 구현과 동작 검증은 아직 시작하지 않았다.

공개 문서·GitHub 조회와 스킬 다운로드는 수행했다. 유료 생성 리소스 생성, 콘텐츠 생성용 유료 API 실행, 실제 SNS 게시는 수행하지 않았다.

## 3. 초기 결정 이력 — 사용자가 확정한 방향

핵심 가치·실사 표현·유동 장수는 유지한다. 표의 시각·영상 관련 미정 사항은 이후 결정과 병행 작업 기록을 함께 확인한다.

| 주제 | 확정 내용 |
| --- | --- |
| MVP 운영자 | 별도의 전문 운영담당자를 두지 않고 개발자가 직접 진행 |
| 핵심 가치 | 브랜드·상품을 일상에 자연스럽게 녹이는 콘텐츠의 매력과 캐릭터 일관성에 중점 |
| 성과의 우선순위 | 운영 효율·제작 시간 단축을 최우선으로 두자는 AI 제안은 채택되지 않음 |
| 캐릭터 표현 | **실제 사람처럼 보이는 가상 인플루언서** |
| 콘텐츠 방향 | **공감, 일상의 공유, 취향 발견** |
| 주요 형식 | 여러 이미지로 구성된 카드형 피드 |
| 카드 장수 | 대략 5장 내외를 생각하지만, 콘텐츠에 따라 더 많거나 적게 구성; 고정 장수로 제한하지 않음 |
| 영상 | 짧은 영상을 넣고 싶다는 의향이 있음; 구현 가능 여부와 MVP 포함 여부는 미확정 |
| 구현 기한 | **2026-09-22 오전까지**; 정확한 시각은 미확정 |

주의: 스타일화된 인간 캐릭터, 게시물당 이미지 3장, 특정 게시물 수 등은 이전 AI 제안이었다. 사용자가 선택한 실사형 표현과 유동적인 카드 장수를 우선해야 한다.

## 4. 최초 요구사항 이력 — 서비스의 필수 흐름과 설계 요구

콘텐츠·승인·복구 원칙은 현재도 유지한다. 이 절의 ‘개발할 예정이다’, ‘범위를 확정하지 않았다’, ‘제한값은 미정’은 요구사항 수집 당시의 상태이며 구현 여부는 맨 앞 요약과 API 문서에서 확인한다.

### 콘텐츠 흐름

1. 피드 주제 선정
2. 스토리보드 작성
3. 게시 문안·해시태그 작성
4. 첨부 이미지 생성 — 콘텐츠 유형에 따라 선택
5. 첨부 영상 생성 — 콘텐츠 유형에 따라 선택
6. 생성 결과를 대시보드에 등록
7. 사람이 수정·승인·반려
8. **승인된 콘텐츠만 SNS에 게시**

대시보드는 별도로 개발하고 백엔드와 API로 연결할 예정이다. 해커톤 중에는 생성 작업을 자주 반복하고, 실제 운영에서는 하루 몇 건 정도를 예상한다. 정확한 생성 주기는 정하지 않았다.

위 흐름은 서비스의 목표 요구사항이다. 내일 오전까지 실제로 구현·시연할 범위는 아직 별도로 확정하지 않았다.

### 콘텐츠와 페르소나

- 페르소나의 외형·성격·말투를 여러 게시물에 걸쳐 일관되게 유지한다.
- 브랜드·상품 정보의 출처와 유효기간을 관리한다.
- 최근 피드와 주제가 중복되지 않도록 한다.
- 가상 인물이라는 사실을 드러낸다.
- 실제 구매·사용 경험을 지어내지 않는다.

### 사람의 수정과 승인 보호

- 사람이 수정한 내용을 늦게 도착한 AI 결과가 덮어쓰지 않아야 한다.
- 승인은 특정 콘텐츠 버전에 연결한다.
- 승인 후 내용을 수정하면 재승인이 필요하다.
- 한 콘텐츠가 승인 대기 중이어도 다른 콘텐츠를 생성할 수 있어야 한다.

### 비동기 실행과 복구

- 이미지·영상 생성은 비동기 작업으로 처리한다.
- 단계별 결과와 외부 생성 작업 ID를 저장한다.
- 프로세스 재시작 시 중단된 단계에서 복구할 수 있어야 한다.
- 재시도 횟수·비용·대기 건수를 제한한다.
- 중복 SNS 게시를 방지한다.

카드 장수를 콘텐츠마다 유동적으로 정하는 요구와 작업 비용·대기 건수를 제한하는 요구는 모두 유지한다. 구체적인 제한값은 미정이다.

## 5. 초기 검토 이력 — 기술·협업 전제와 당시 미정 사항

다음은 초기 선택지 목록이다. 로컬 Python·SQLite·웹 대시보드, 첫 페르소나, Instagram 대상, 5분 heartbeat 등 이후 정한 항목을 다시 미정으로 되돌리지 않는다. AWS·공동 원격 운영과 검증되지 않은 SDK 방식은 실제 필요 시 별도로 확인한다.

### AI 실행 방식

- Codex를 사용할 계획이다.
- **개발 자동화용 랄프 루프와 서비스의 콘텐츠 생성 스케줄러는 구분한다.**
- 운영 에이전트에 Codex SDK를 사용하는 방법을 논의했지만, 최종 구현 방식은 정하지 않았다.
- 이미지·영상 생성에는 연결된 생성 도구 또는 API가 필요하다.
- 기존 웹 대화에서 나온 SDK 코드는 설명용 예시이며 실행·검증되지 않았다.
- 모델명, SDK 메서드, 인증 방식, 사용 권한은 구현 전에 현재 공식 문서와 실제 환경에서 확인해야 한다.

### 협업·인프라

- GitHub로 4명이 협업할 예정이다.
- AWS 계정 하나에 배포하는 방안과 한 사람의 맥북을 공용 실행 서버로 사용하는 방안을 비교 중이다.
- 원격 접속으로 논의한 것은 일반 SSH가 아니라 **Codex Remote**다.
- 서로 다른 계정을 쓰는 4명이 Codex Remote로 한 맥북을 공유할 수 있는지는 확인하지 않았다.
- SQLite는 단순한 초기 DB 후보이며, Redis도 처음에 검토했다. 어느 쪽도 확정되지 않았다.
- AWS의 `EC2 + SQLite + S3`, 맥북의 `SQLite + 로컬 미디어 폴더`는 제안일 뿐이다.
- React + Vite 등 프론트엔드 기술 스택도 제안일 뿐이다.

### 당시 아직 선택하지 않은 항목

- 프론트엔드·백엔드 언어와 프레임워크
- 배포 환경과 공동 작업 방식
- 게시할 SNS 및 실제 계정
- 페르소나의 이름·연령대·성별·외형·생활 배경·구체적 취향
- 첫 시연에 사용할 GS 계열사·브랜드·상품
- 이미지·영상 생성 모델과 사용할 도구·API
- API 예산과 생성 한도
- 정확한 생성 주기
- MVP에서 실제로 연결할 생성·게시 단계

## 6. 초기 제안 이력 — 당시 합의 전 구현안

다음 내용은 최초 AI 제안과 당시 진행 상태를 보존한 것이다. 아래 ‘디렉터리 미생성’, ‘DB 스키마 미작성’, ‘답변 대기’는 현재 상태가 아니다. 실제 구현 구조는 README와 API 문서를 따른다.

### 최소 구조 제안

```text
boca/
  apps/
    dashboard/       # 콘텐츠 검토·수정·승인 화면
    backend/         # API, 콘텐츠·버전·승인·작업 관리
    worker/          # 생성·게시 작업의 비동기 실행
  contracts/         # API 명세와 공통 상태·요청·응답 형식
  docs/              # 요구사항, 설계 결정, 시연 시나리오
  data/              # 공개·가상 데이터와 출처 정보
```

위 애플리케이션 디렉터리들은 아직 생성하지 않았다. 현재 프로젝트에는 이 인계 문서, `SKILLS.md`, `.agents/skills/persona-content/`의 스킬 및 메타데이터 파일이 있다.

### 구현 순서 제안

1. 내일 오전의 완료 기준과 시연 범위를 확정한다.
2. 페르소나와 첫 콘텐츠를 정의하고, B·C 중심의 결과물 평가 기준을 정한다.
3. API 계약과 데이터·상태 전환 구조를 정의한다.
4. 모의 응답으로 생성 → 수정 → 버전별 승인 → 모의 게시 흐름을 연결한다.
5. 실제 이미지 생성 등을 연결해 콘텐츠 품질과 일관성을 검증한다.
6. 남은 시간과 도구 연결 상태를 확인해 영상 및 실제 SNS 연동 범위를 결정한다.

초기에 제안한 주요 저장 항목은 페르소나 버전, 상품 출처·유효기간, 콘텐츠 버전, 단계별 결과, 외부 작업 ID, 승인 기록, 게시 기록이다. DB 스키마는 아직 작성하지 않았다.

### 당시 시간 제약에 대한 제안

- 카드형 피드의 완성도와 캐릭터 일관성을 먼저 확보한다.
- 짧은 영상은 별도 확장 목표로 검토한다.
- 실제 생성부터 대시보드 수정·승인까지 동작하는 범위를 MVP 목표로 제안했다.
- 실제 SNS 연동을 뒤로 미루는 안을 제안했다.

**영상이나 실제 SNS 게시를 MVP에서 제외하기로 확정한 것은 아니다.** 사용자의 답변을 기다리는 상태다.

## 7. 과거 질문 이력 — Q6~Q8

이 질문들은 이후 사용자 답변으로 해소되었다. 질문과 당시 제안을 기록으로 보존하며 다시 질문하지 않는다. 현재 반영된 답변은 실제 생성·수정·승인과 조건 충족 시 게시 요청, 09:00 생성 종료, 여의도·목동 워킹맘과 다섯 살 딸, 일상 이야기 먼저·GS 소재 연결이다.

### Q6. 내일 오전의 완료 기준과 정확한 시각

다음 중 어디까지 보여줘야 하는가?

- A. 완성도 높은 콘텐츠 샘플과 서비스 설명
- B. 실제 생성 버튼 → 콘텐츠 생성 → 대시보드 수정·승인이 동작하는 시스템
- C. B에 실제 SNS 게시까지 연결된 시스템

AI 추천은 B였지만 사용자는 아직 선택하지 않았다. 2026-09-22 오전 중 정확한 마감 시각도 확인해야 한다.

### Q7. 구체적인 페르소나

정해둔 인물 설정이나 참고 이미지가 있는가? 없다면 연령대·성별·직업 또는 생활환경·주요 취향을 어떻게 정할 것인가?

AI는 ‘20대 후반~30대 초반 직장인, 퇴근 후와 주말에 먹거리·집 꾸미기·작은 취향을 발견하는 인물’을 제안했지만 채택되지 않았다. 확정된 것은 **실사형 가상 인플루언서**와 **공감·일상 공유·취향 발견**이라는 방향이다.

### Q8. 첫 콘텐츠와 브랜드·상품의 관계

첫 시연에 반드시 등장해야 하는 GS 계열사·브랜드·상품이 있는가? 아니면 캐릭터의 일상 주제에 맞는 상품을 나중에 고를 것인가?

AI는 일상 이야기를 먼저 정하고 어울리는 상품을 선택하는 안을 제안했지만, 아직 결정하지 않았다.

## 8. `grill-me` 설치 및 대화 방식

사용자의 요청으로 로컬 Codex 환경에 `grill-me`를 설치했고, 이 대화에 적용했다.

- 출처: [mattpocock/skills](https://github.com/mattpocock/skills)
- 설치 기준 커밋: `c55ee46073ed923f86ce59a5eb3b6d895095d1b7`
- 설치한 원본 경로: `skills/productivity/grill-me`, `skills/productivity/grilling`
- 사용자 로컬 설치 위치: `~/.agents/skills/grill-me/`, `~/.agents/skills/grilling/`
- 현재 `grill-me`는 실제 질문 진행을 `grilling`에 위임하는 구조다.
- Codex에서 사용할 수 있도록 `grill-me/SKILL.md`의 전용 `Skill` 도구 호출 문구를 `../grilling/SKILL.md`를 읽는 방식으로 수정했다.
- `disable-model-invocation` 프런트매터를 제거하고, 명시적 호출 정책은 `agents/openai.yaml`의 `allow_implicit_invocation: false`로 유지했다.
- 두 스킬의 형식 검증과 메타데이터·참조 경로 검증을 통과했다. 이후 대화에서 질문 진행에 사용했다.

설치는 현재 사용자의 컴퓨터에만 적용되어 있다. **이 Markdown 파일을 공유한다고 다른 사용자의 환경에 스킬이 설치되지는 않는다.** 다른 사용자도 필요하면 같은 원본의 두 스킬을 설치해야 한다.

이후 사용자 요청으로 `frontend-design`, `playwright`, `remotion-best-practices`도 사용자 환경에 설치했다. 기존 `imagegen`, `skill-creator`, `openai-docs`는 유지했고, `persona-content`는 프로젝트 내부에 새로 작성했다. 자세한 출처와 사용법은 [스킬 연결 내역](SKILLS.md)을 참고한다.

대화는 다음 원칙으로 이어간다.

- 확정된 배경을 반복해서 묻지 않는다.
- 결정의 선후 관계를 고려해 지금 답할 수 있는 질문을 묶어서 제시한다.
- 질문마다 추천안과 이유를 함께 제시하되, 추천을 사용자 결정으로 간주하지 않는다.
- 코드·환경에서 확인할 수 있는 사실은 직접 확인한다.
- B·C 중심의 우선순위와 내일 오전까지라는 기한을 반영한다.

## 9. 과거 인계 지침 — 최초 기획·인계 단계

아래는 구현 전 작성한 지침 원문이다. ‘구현 전’, ‘이후 실행 허용 지시 없음’, ‘Q6~Q8 답변 받기’는 이후 요청과 구현으로 갱신되었다. 현재 작업은 맨 앞 요약과 최신 사용자 지시를 따른다. 유료 리소스와 게시 권한은 구체적인 최신 요청·연결 상태를 확인하며 추정하지 않는다.

- 현재 단계는 기획 구체화와 인계이며, 구현이 완료됐다고 가정하지 않는다.
- 실사형 인물과 유동적인 카드 장수는 사용자 결정이므로 임의로 변경하지 않는다.
- 영상은 희망 기능이며, 구현 가능 여부를 확인하지 않은 상태다.
- 폐기한 MD-EYE 아이디어로 되돌아가지 않는다.
- Codex SDK 예시, 기술 스택 제안, 인프라 제안을 검증된 구현으로 취급하지 않는다.
- 당시 사용자는 현황 파악 중 유료 리소스 생성이나 실제 SNS 게시를 시작하지 말라고 요청했다. 이후에도 해당 실행을 허용하는 지시는 없었다.
- 다음 행동은 **Q6~Q8의 답변을 받아 시연 범위와 첫 콘텐츠를 구체화하는 것**이다. 답변에 따라 기술 선택과 실제 구현 순서를 정한다.

## 구현 이력 — 2026-09-21, 버튼으로 제어하는 연속 제작

이 단계에서 확정한 사용자 요청은 10건에서 멈추지 않는 반복이다. 대시보드의 「콘텐츠 자동 생성」 버튼으로 GS 소재 확인→일상 기획→스토리보드→문안→미디어→검수→피드 등록을 계속 순환하고, 실제 단계별 로그를 보존한다. 기존 10건 제한이나 목표 달성 후 신규 생성 중단 문구는 폐기한다. 오늘 17:30~내일 09:00 일정과 사람 승인 후 게시 원칙은 유지한다.

연속 제작 동작과 5분 heartbeat는 현재도 적용된다. 아래의 ‘다중 페르소나 생성 미구현’과 당시 단일 화면 구성은 이후 종합 스튜디오 구현으로 갱신되었다.

반영 사항:
- config/overnight.json: continuous, stop_after_posts=null, min_posts_reference=10. 미디어는 이야기별 유동 장수.
- apps/backend/automation.py: SQLite의 production_runs/cycles/events/source_materials, 단계별 jobs, 소유권과 체크포인트, 외부 ID, 실패·재시도·중단 복구, 중복 실행 방지.
- apps/dashboard: 시작·일시정지·재개·중지, 한국 시간 일정, 작업자 상태와 단계, 누적 로그와 오류 필터, GS 소재 입력·출처·유효기간·노출 범위, 3열 피드/버전 승인 유지.
- 실제 도구 호출은 이 대화의 Codex 예약 작업자가 실행한다. 서버 버튼은 실행 기록을 저장한다. 대기와 실제 생성은 별도로 표시한다. 기존 boca-10 예약의 표시명과 프롬프트를 연속 생성으로 변경하고 5분마다 확인하도록 업데이트했다. id의 10은 과거 식별자이며 총량 제한이 아니다.
- 기존 승인된 오늘 밤 일정의 실행을 DB에 예약 등록했다. 생성 전 실제 시각 확인 및 사용자 중지 상태를 우선한다. 도구 호출·이미지 생성 성공을 예약 등록 성공과 혼동하지 않는다.
- 작업자가 끊기면 실제 외부 결과 확인 전 재호출하지 않는다. worker-inspect로 복구 자료 조회, worker-checkpoint로 부분 결과 보존 및 안전한 작업 인계가 가능하다.
- 다중 페르소나 생성·계정 선택/연결·실제 Instagram 게시 워커는 아직 미구현이다. 유료 리소스 생성, 실제 SNS 게시, 사람 승인 대행은 수행하지 않았다.

운영 시 docs/overnight-runbook.md와 docs/local-api.md를 따른다. 이 변경의 기능 검증은 격리 DB/더미 파일로 수행하며 테스트 피드를 실제 완성 콘텐츠로 세지 않는다.

## 2026-09-21 추가: Segmind Pro 영상 연동

이 절은 병행 영상 작업이 남긴 별도 기록으로 보존한다. 이번 종합 스튜디오 인계 정리에서는 아래 모델·비용·포트·인증·테스트 수·생성 결과를 재검증하지 않았다. 현재 사실 여부와 추가 실행 권한은 해당 작업 및 연결 환경에서 확인한다.

사용자는 영상 길이를 30초 내외로 정했고, Segmind Pro 구독 가입 후 연동을 요청했다. 현재 Instagram 계정은 개인 계정이며, 프로페셔널 전환·Meta 인증·실제 게시 연결은 완료되지 않았다.

- 별도 영상 제작 화면 `/video`와 Segmind REST 연동을 구현했다. 기본 모델은 Seedance 2.5, 30초, 720p, 세로 9:16이다.
- 기존 스튜디오 상단의 영상 제작 링크로 이동한다. 현재 추가 실행 서버는 http://127.0.0.1:8767/video 이며 기본 실행 포트는 기존대로 8765다.
- API 키는 화면에서 입력해 계정 인증만 조회한다. `.runtime/segmind-api-key`에 0600으로 저장하며 응답·로그·Git에 키를 포함하지 않는다. 구현 완료 시점에는 실제 키가 아직 등록되지 않아 계정 인증 및 실제 생성은 미검증이다.
- 영상 버튼을 눌렀을 때만 별도 비동기 작업을 만든다. 기존 Codex 연속 이미지 제작과 비용 정책은 유지한다. 생성 요청 중복 방지, 외부 ID 저장, 불확실한 요청의 자동 재전송 금지, 기존 ID 결과 조회 복구, 비용·대기 한도를 구현했다.
- 완성 영상은 기존 이미지 콘텐츠와 별도의 검토 대기 콘텐츠로 보존한다. 원본의 사람 수정과 승인을 덮어쓰지 않으며 영상 수정도 재승인 대상이다.
- 기본 비용 추정은 30초 720p 약 $7.17이며 한 편 $8·UTC 하루 $15·대기/진행/확인 필요 2건으로 제한한다. 실제 계정 전체 지출 상한을 보장하는 기능은 아니다.
- 외부 API 대역과 격리 DB를 사용해 전체 81개 테스트 통과. 유료 영상 생성·실제 Instagram 게시를 수행하지 않았다.

연결 절차, API 계약과 재시작 복구는 [Segmind 영상 연동](docs/segmind-video.md)을 참고한다. 생성 전에는 페르소나 기준 이미지 또는 기존 이미지 콘텐츠도 필요하다.

## 2026-09-21 추가: 영상 제작 참고 MD

사용자가 영상 제작 메뉴에서 MD를 등록해 자동 영상 제작의 참고 지침으로 사용하도록 요청했다. `/video`에 파일 등록·본문 미리보기·동일 파일명 버전 교체·적용 켜기/끄기·삭제를 추가했다. 지침은 모든 페르소나의 새 수동·자동 영상 작업에 공통 적용된다. 스토리보드와 함께 Segmind 프롬프트로 전달하며, 파일 본문·버전·해시와 최종 프롬프트를 작업 접수 시 저장한다. 이후 파일 변경·삭제·재시작은 기존 영상의 입력을 바꾸지 않는다. 제작 기록에서 당시 적용한 MD를 확인한다.

UTF-8 `.md`, 파일당 64KB·12,000자, 활성 파일 합계 12,000자, 최대 10개다. 상세 API와 사용법은 docs/segmind-video.md에 있다. 전체 테스트 112개 통과 및 격리 브라우저에서 업로드·미리보기·적용 전환·삭제·새로고침 유지 검증 완료. 실제 8767 서버와 화면을 갱신했고 기존 완료 영상 1건과 작성 중인 문안은 보존했다. 이 기능 검증에서 새 유료 영상이나 자동 제작 실행을 시작하지 않았다. 실제 등록할 MD는 사용자가 화면에서 선택한다.

## 2026-09-21 추가: 참고 MD를 콘텐츠 제작으로 이동

영상 참고 MD의 관리 위치를 **콘텐츠 제작 → AI 자동 제작**으로 옮겼다. AI 시작 폼 근처에 적용 파일 수와 등록·관리 이동 버튼을 표시하며 기존 `/video`는 같은 관리 화면으로 연결한다. 기존 파일 DB와 직접 영상 생성 적용은 유지한다. 자동 제작은 콘텐츠 순환 시작 시 활성 MD를 `production_cycle_video_guides`에 고정하고 각 단계의 `job.input_json.video_guides`로 전달한다. 기획·스토리보드·검수와 최종 영상 요청이 동일한 지침을 사용한다. 수정은 다음 콘텐츠부터 반영되며 이미지 중심 실행에는 영상 MD를 적용하지 않는다. 재시작 후에도 진행 중인 콘텐츠의 지침은 유지한다. 전체 테스트 122개 통과 및 격리 화면에서 파일 선택·갱신 중 선택 유지·등록을 검증했다. 기존 중지 실행을 시작하지 않고 기능만 반영한다.

## 2026-09-21: Low MD influence

User requested lower MD influence to avoid similar videos. AI stage inputs now include video_guide_policy; video snapshots include guide_policy. Both prioritize the current story and persona, select at most two optional atmosphere cues across all MD files, allow omitting unsuitable references, and discourage copying example topics, hooks, props, shot sequences, fixed shot counts or endings. Quality review does not require all MD items. Identity, factuality, disclosure and human approval remain required. Policy is frozen per content cycle and restored on restart. This is a prompt policy, not a numeric model weight or a measured diversity guarantee. Existing saved prompts and original MD files remain unchanged. All 124 tests passed; the live UI shows low influence and retains four MD files. No new video or stopped automation was started.

## 2026-09-21 추가: 자동 영상 480p·20초 기본값과 옵션

콘텐츠 제작 → AI 자동 제작에 **자동 영상 설정**을 추가했다. 기본값은 480p·20초이며 해상도 480p/720p, 길이 20~30초를 선택할 수 있다. 영상 기본값 저장은 `POST /api/settings`의 `generation.auto_video`로 SQLite에 보존하며 생성을 시작하지 않는다. 실행 시 선택한 `video` 옵션은 `run.settings.video`에 고정하고 각 단계의 `video_settings`와 실제 Segmind 요청에 동일하게 전달한다. 기본값을 나중에 저장해도 진행 중 실행은 바뀌지 않는다. 기존 실행에 다른 옵션으로 시작 요청하면 409다. 옵션 도입 전 실행과 수동 영상은 기존 공통 기본값을 유지한다.

실제 화면에서 옵션 전환·예상 비용 갱신·이미지 중심 선택 시 숨김·480p/20초 저장을 확인했다. 전체 테스트 149개 통과, JavaScript 문법 검증 완료. 현재 실 DB 기본값은 480p·20초이며 기존 실행은 중지, 영상 작업 1건, 일일 예상 한도 $15를 유지한다. 이 변경 검증에서 새 유료 영상 생성 또는 SNS 게시를 수행하지 않았다. 영상 비율 1/3은 앞선 비용 시뮬레이션이며 강제 배분 규칙은 이번 변경에 포함하지 않는다.

## 2026-09-21 추가: 전문가 팀 상태 갱신

입력창 포커스·MD 등록·미디어 재생 중 전체 화면 렌더링을 보류하면 전문가 팀도 멈추던 문제를 수정했다. 서버 상태를 받은 즉시 팀 패널을 별도로 갱신하고 상세 산출물 로딩을 기다리지 않는다. `/api/state`의 `specialists.cycle/stage_jobs`로 현재 순환과 단계 상태를 제공해 인계 사이에도 완료 배정을 표시한다. 재작업·새 순환에 이전 완료 상태를 붙이지 않는다. 5초 주기의 마지막 확인 시각, 작업자 연결 대기/지연, 전문가 배정 대기, 실제 작업 중, 완료, 연결 오류를 구분한다.

신고 당시 run-5239919e705345fa는 실행 요청만 접수된 상태로 cycle 0, heartbeat 없음, 실제 배정 0건이었다. 조사 중 실행이 stopped로 바뀌었으며 재시작하지 않았다. 격리 DB/브라우저에서 입력 포커스를 유지한 채 리서처 완료 → 기획자 작업 중 자동 전환을 검증했다. Python 171개 및 Node 화면 상태 회귀 테스트 5개 통과. 실제 8767 서버·탭에 반영했고 유료 생성이나 운영 배정은 만들지 않았다.
